const { FIELD_TYPES } = require('bfx-hf-models')

module.exports = {
  id: FIELD_TYPES.NUMBER,
  price: FIELD_TYPES.NUMBER,
  amount: FIELD_TYPES.NUMBER,
  mts: FIELD_TYPES.NUMBER,
  symbol: FIELD_TYPES.STRING,
}
